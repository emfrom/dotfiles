# Your's truly
[user]
	email = vertlaforet@protonmail.com
	name = emfrom
[github]
	user = emfrom
# Reasonable config
[init]
	defaultBranch = main
[credential]
	helper = store

# Hard remove CRLF's
# Needed as I've just "moved out" a bunch of stuff from windows
[core]
	autocrlf = false
[filter "tabs-and-crlf"]
    clean = "sed -e 's/\\t/  /g' -e 's/\\r$//'"
    smudge = "sed -e 's/\\t/  /g' -e 's/\\r$//'"

